# Kamal deployment config (https://kamal-deploy.org). Fill in your host/registry.
service: {doido_name}
image: your-user/{doido_name}

servers:
  - 192.168.0.1

proxy:
  ssl: true
  host: app.example.com

registry:
  username: your-user
  password:
    - KAMAL_REGISTRY_PASSWORD

env:
  clear:
    DOIDO_ENV: production
