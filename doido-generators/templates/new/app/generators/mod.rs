//! App-owned code generators (doido). Each `doido generate generator <Name>`
//! adds its module here — just above the marker — and registers it on the
//! `Doido` builder in `src/main.rs`.

// @generated-generators
