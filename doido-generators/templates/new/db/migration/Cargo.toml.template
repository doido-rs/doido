[package]
name = "migration"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
async-trait = "0.1"
tokio = { version = "1", features = ["full"] }
# SeaORM + migration helpers (create_table, add_column, …) via doido-model.
doido-model = {doido_model_dep}
