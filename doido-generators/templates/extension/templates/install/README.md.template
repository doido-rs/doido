# {doido_ext_pascal} extension

This file was generated by `{doido_ext_snake}:install`.

Edit this generator in `{doido_ext_name}` (`src/generators/install.rs`) to emit
migrations, config snippets, routes, or other app files when users run:

```sh
cargo doido generate {doido_ext_snake}:install
```
