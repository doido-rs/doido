# Ralph / loop progress log — Doido "close the Rails 8 gap" backlog
# One line per completed story: US-XXX: <what changed>
# Backlog: harness/prd.json (seeded from docs/RAILS8-GAP-ANALYSIS.md, 2026-07-24).
# The previous finishing run (a different US-001..009) is archived under
# harness/archive/2026-07-24-finish-doido-progress.txt.
---
US-001: CookieSessionStore signs sessions into an HMAC-SHA256 cookie (encode/decode, tamper+wrong-secret rejection); Session typed get/set. make verify green.
US-002: CacheSessionStore — server-side sessions backed by any doido_cache::CacheStore (id in cookie, data in cache, optional TTL). make verify green.
US-003: config-driven CORS — [middleware.cors] in YamlConfig + MiddlewareStack::with_cors_config builds a CorsLayer (opt-in). make verify green.
US-004: host authorization — MiddlewareStack::with_allowed_hosts 403s unlisted Host headers (port-stripped); empty=allow-any. make verify green.
US-005: configurable middleware insertion — MiddlewareStack::insert_before/insert_after router transforms around the always-on stack. make verify green.
US-006: strong parameters — params::Params with require/permit/deserialize + Context::query_params(). make verify green.
US-007: CSRF protection — csrf module + MiddlewareStack::with_csrf() double-submit-cookie guard (constant-time compare). make verify green.
US-008: flash messages — flash::Flash carried in a signed cookie (set now / read next / then gone). make verify green.
US-009: cookies API — cookies::CookieJar (plain + HMAC-signed) + shared signing module. make verify green.
US-010: rescue_from — rescue::RescueHandlers maps typed errors to responses via downcast (else default 500). make verify green.
US-011: filter skipping — #[controller] before/after_action gain except=[...] + #[skip_before_action(fn)]. make verify green.
US-012: around_action — #[around_action(fn)] wraps the action via an async closure (fn brackets + owns the response). make verify green.
US-013: respond_to — Context::negotiated_format()/respond_to() format negotiation (Accept + .json/.html), RespondTo html/json branches, 406 fallback. make verify green.
US-014: HTTP caching — Context::fresh_when(etag, last_modified) conditional-GET 304 via If-None-Match/If-Modified-Since. make verify green.
US-015: rate limiting — rate_limit::RateLimiter cache-backed fixed-window check(key). make verify green.
US-016: force_ssl — MiddlewareStack::with_force_ssl() 301-redirects insecure requests to https. make verify green.
US-017: send_data/send_file — Context::send_data + async send_file downloads. make verify green.
US-018: validations — validation::{Errors, Validate} with presence/length validators (Active Record style). make verify green.
US-019: model callbacks — callbacks::Callbacks trait + run_create/update/destroy runners (Rails ordering, abort on before-hook error). make verify green.
US-020: association ergonomics — association::Association conventions (fk/table) + HABTM join_table via Inflector. make verify green.
US-021: named scopes — scope::Scoped extension (.scope/.scope_if) for chainable sea-orm query fragments. make verify green.
US-022: secure password/tokens — password module (bcrypt hash/verify, HasSecurePassword::authenticate, generate_token). make verify green.
US-023: fixtures/factories — factory::{Factory, sequence} test-record builders. make verify green.
US-024: normalizes — normalization::Normalizer chainable attribute normalization. make verify green.
US-025: as_json/serialized columns — serialization module (as_json only/except + serialize/deserialize_column). make verify green.
US-026: polymorphic/STI — PolymorphicAssociation ({name}_type/id) + sti_type_column conventions. make verify green.
US-027: multiple databases — databases::Databases writing/reading role routing with writer fallback. make verify green.
US-028: transaction wrapper — transaction(conn, work) commit-on-Ok/rollback-on-Err helper. make verify green.
US-023: attribute enums — enums::AttributeEnum (label/all/from_label/index/labels). make verify green.
US-029: generator e2e — every generator output compiles (CLI-driven make example). Added job/mailer app wiring + deps; fixed doido_cable::channel macro export. make verify + make example green.
US-030: root route — root!(handler) in the routes! DSL maps GET /. clippy+test green.
US-032: named route helpers — get!/post! accept `as: name` (parity with resources helpers). clippy+test green.
US-033: member/collection — resources! member:/collection: extra routes. clippy+test green.
US-034: route constraints — constraints module (format validators + Constraints matcher for filters). clippy+test green.
US-035: singular resource — resource!(name, Ctrl) no-id routes. clippy+test green.
US-036: shallow nesting — shallow_resources! nested collection + shallow member routes. clippy+test green.
US-037: redirect routes — redirect!(from, to) 301 in the DSL. clippy+test green.
US-038: glob routes — axum {*name} catch-all via get! passthrough (test). test green.
US-039: mount — mount!(path, router) nests a sub-router. make verify green (router phase complete).
US-040: partials — render_partial + render_collection helpers. clippy+test green.
US-041: content_for — ContentFor named-block capture/yield store. clippy+test green.
US-042: form builders — helpers::form (form_tag + field helpers, escaped). clippy+test green.
US-043: link helpers — helpers::link (link_to/link_to_class/button_to). clippy+test green.
US-044: asset helpers — image_tag/stylesheet_link_tag/javascript_include_tag. clippy+test green.
