//! Controller helpers (doido-controller). Each `doido generate helper <Name>`
//! registers its module here, just above the marker below.

mod application_helper;

pub use application_helper::ApplicationHelper;

// @generated-helpers
